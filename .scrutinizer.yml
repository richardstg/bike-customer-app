checks:
  javascript: true
build:
  nodes:
    analysis:
      tests:
        override:
          - js-scrutinizer-run
          command: "npm test"
  # tests:
  #   override:
  #     command: "npm test"
